The core of Lash. Each component is a separate addon that can be dropped into any project for maximum reuse.

Definitions:
layer: An array of frames
timeline: An array of layers(?)
frame: The smallest element of an animation. It corresponds to an image within a layer.
keyframe: An image that can be held for multiple frames within a layer.
